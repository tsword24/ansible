- name: argument variables
  hosts: local
  connection: local
  tasks:
  - name: course info
    ansible.builtin.debug:
      msg: "The {{ COURSE }} course is  {{ DURATION }} of duration and the teaching mode is {{ MODE }}"


#  ansible-playbook -i inventory.ini -e ansible-user=ec2-user -e ansible-password=DevOps321 09-argument-vars.yaml -e "COURSE=DEVOPS" -e "DURATION=10HRS" -e "MODE=ONLINE"
